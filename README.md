# fastify-scaffold

## project setup

**dependencies**. `npm i` <br>
**env**. Copy the `.env.example` as `.env` file and setup your variables. <br>
**database**. Using knex-cli execute `migrations` and `seeders`<br>